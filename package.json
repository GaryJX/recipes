{
  "name": "recipes",
  "version": "1.0.0",
  "private": true,
  "description": "A static recipe site generated from markdown files.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "node --run build && node scripts/serve.mjs",
    "start": "node --run serve"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/GaryJX/recipes.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "marked": "^15.0.12"
  }
}
